<!-- filename: runs/<slug>-<date-time>.md — slug-keyed so accumulated runs stay
     attributable to their work item wherever they land (the default branch in
     `include` mode; off the public branch in `exclude`) -->
## Goal
## Context read
## Actions taken
## Branches touched
## Verification
## Follow-up
## Need human context
## Promotion candidates
